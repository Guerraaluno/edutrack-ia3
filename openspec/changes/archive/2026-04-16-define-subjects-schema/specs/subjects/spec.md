## MODIFIED Requirements

### Requirement: Subject storage
The system SHALL store subjects with an identifier, a name, teacher, hours, user association, and an active/inactive state.

#### Scenario: Load subjects from backend
- **WHEN** the user opens the Disciplinas page
- **THEN** the app loads subjects from the backend API and displays each subject's name, teacher, and hours
